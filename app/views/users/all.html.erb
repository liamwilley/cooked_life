<div class="row white-text" style="background-image: url(<%=image_path 'backgrounds/bg-aside.jpg'%>)">
<div class="col-md-2"></div>
  <div class="col-md-8">

  <div class="row new-meal-form">
      <div class="col-md-6">
        <h3 class="white-text my-text-left login-center chef-size">Chefs! </h3>
      </div>
      <div class="col-md-6">
        <a href=<%="/users/#{current_user.id}"%> class="btn btn-block btn-full-width">My Profile</a href="">
      </div>
    </div>

<!-- 

    <h1 class="my-text-left">Chefs!</h1><h2 class="my-text-right"><%= link_to "My Information", "/users/#{current_user.id}" %></h2>
    <!<p class="my-text-right"><%= link_to "My Information", "/users/#{current_user.id}" %></p> -->
    
    <table class="table">
      <tr>
        <th>Name</th>

        <th>Email</th>
      </tr>
      <% if !@users.empty? %>
          <% @users.each do |user| %>
        <tr> 
          <td><%= link_to user.full_name, "/users/#{user.id}" %></td>
         
          <td><%= user.email %></td>
          <% end %>
        <% else %>
          <td>No information passed through</td>
        </tr>
        <% end %>
    </table>
  </div>
</div>

